#TODO to be tested
[source]
tar = "https://ftp.gnu.org/gnu/ncurses/ncurses-6.5.tar.gz"
[build]
dependencies = [
    "relibc"
]
template = "custom"
script = """
COOKBOOK_CONFIGURE_FLAGS+=(
    --disable-widec
)
cookbook_configure
"""
